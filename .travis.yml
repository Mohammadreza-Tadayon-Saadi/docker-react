language: node_js
node_js:
  - 18
  
sudo: required

services:
  - docker

before_install:
  - docker build -t tedi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true tedi/docker-react npm run test -- --coverage